configfile: "config/config.yaml"


import textwrap


rule all:
    input:
        expand(
            "results/{genome}/flanking",
            genome = config["input"]["genomes"].keys()
        )


include: "rules/pfam_search.smk"
include: "rules/tnpb_cluster.smk"
